services:
  web:
    build: .
    command:  >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - ${WEB_PORT}:8000
    environment:
      DATABASE_HOST: 192.168.0.5
      DATABASE_PORT: 4321  # Default PostgreSQL port
      DATABASE_NAME: cookbook
      DATABASE_USER: python
      DATABASE_PASSWORD: Skytak++102